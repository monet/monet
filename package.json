{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --hoist",
    "lint": "lerna run lint",
    "lint-specs": "lerna run lint-specs",
    "mocha": "lerna run mocha",
    "tsc": "lerna run tsc",
    "test": "npm run lint && npm run lint-specs && npm run tsc && npm run mocha "
  },
  "devDependencies": {
    "@types/node": "^12.11.6",
    "@ulfryk/tslint-config": "^1.0.2",
    "lerna": "^3.18.3",
    "tslint": "^5.20.0",
    "typescript": "^3.6.4"
  }
}
